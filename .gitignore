.env
# faiss_store/ est versionne (5 MB) pour garantir que le Docker image
# et HF Spaces ont toujours le vector store pret a l'emploi.
data/raw/
__pycache__/
*.pyc
.ipynb_checkpoints/
venv/
venv311/
*.egg-info/

# Artefacts ML regeneres en CI (voir scripts/train_nb10.py)
outputs/NB10_*.joblib
outputs/NB10_predictions_2030_*.csv
mlruns/
mlflow.db
outputs/NB10_mlflow_*_grid.csv

# Etat local Chainlit (ne jamais committer)
data/chainlit.db

# Jobs planifies APScheduler (local)
scheduler_jobs.db

# BAT teaser (apercu local, pas d'interet sur le repo)
outputs/teaser_preview.html
outputs/teaser_preview.txt

# Logs de generation PDF (mdpdf, etc.)
mdpdf.log
*.log

# Diagramme architecture HTML (regenere via Claude Web, non versionne tant qu'il
# n'est pas finalise et coherent avec la doc)
SAEARCH_architecture.html
